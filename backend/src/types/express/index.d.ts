// Add custom variables to various express objects like Req and Res

export {};

declare global {
  namespace Express {
    // export interface Request {
    //   user?: User;
    // }
    // export interface Response {
    //   user?: User;
    // }
  }
}
